# PROGRESS LOG — CPF Semantic Search PoC

## ที่มาโปรเจกต์

อ่านบทความ CPF เรื่อง Open Innovation และผลิตภัณฑ์ MEAT ZERO (เนื้อจากพืช) เกิดไอเดียอยากทำโปรเจกต์ที่ไม่ได้แก้ปัญหาของ CPF ตรงๆ แต่เอาผลิตภัณฑ์ที่ดีอยู่แล้วมาทำให้ค้นหา/เข้าถึงง่ายขึ้น

เป้าหมาย: เสนอโปรเจกต์นี้ให้ CPF ในบริบทที่ CPF ต้องการรับเด็กเทค (tech talent)

## Pain point ที่พบ (มีหลักฐาน screenshot จริงจาก ALL Online ทุกข้อ)

**กลุ่ม A — แก้ด้วย semantic search / embedding (ขอบเขตของ PoC นี้)**

1. **Semantic gap** — พิมพ์ "ของกินเจไม่ใช่ผัก" ได้ "ไม่พบข้อมูล" ทั้งที่ MEAT ZERO มีสินค้าตรงความต้องการขายอยู่จริง
2. **Precision ต่ำ** — พิมพ์คำตรงตัวแต่ผิดบริบท เช่น "วีแกน" ได้แชมพู Dove 18 รายการ, "plant-based" ได้อาหารสุนัขปนมาใน 434 รายการ, "เกี๊ยวซ่า" ได้ลูกอม Yupi
3. **Discovery gap** — ผู้ใช้ที่ไม่รู้จักชื่อแบรนด์มาก่อน มักไป Google หาก่อน แล้วค่อยเอาชื่อที่ได้มาพิมพ์ค้นในแอป แถมข้อมูลที่เจอใน Google บางครั้งล้าหลังไปแล้ว
4. **Exact-match brittleness** — พิมพ์ชื่อสินค้ายาวๆ ขาดไปแม้แค่คำเดียว (เช่น "ราด" หายไปจาก "ข้าวราดผัดพริกแกงหมูกรอบเจ") ก็หาไม่เจอทันที

**กลุ่ม B — ปัญหาข้อมูล/metadata (ข้อเสนอแยก ไม่ใช่สิ่งที่ embedding แก้ได้)**

5. **Brand tagging ไม่ครบ** — filter แบรนด์ "Meat Zero" เจอแค่ 1 ใน หลายรายการที่มีขายจริง
6. **Category ไม่ถูก tag** — พิมพ์ "อาหารแช่แข็ง" ไม่เจอผลลัพธ์ ทั้งที่สินค้าจริงอยู่ในหมวดนี้
7. **(พิจารณาตัดออก)** ระบบค้นหาแยกกันในแต่ละช่องทางของเครือ CP (7-Eleven, Makro, Lotus's, CP Freshmart) — มีความเสี่ยงด้าน business (ราคา/เงื่อนไขต่างกันจริง) จึงตัดสินใจไม่เสนอเป็น pain point หลัก

## การตัดสินใจสำคัญระหว่างทาง

- เลือกทำ semantic search ก่อน (ไม่ทำ dietary scanner หรือรวมสองอย่าง)
- ปรับจาก "ใส่ search ในแอป" เป็น "เครื่องมือถาม-ตอบธรรมชาติ" ที่แทนขั้นตอน Google search ก่อนเปิดแอป
- ตัด scope ให้ PoC โฟกัสเฉพาะกลุ่ม A (embedding แก้ได้จริง) กลุ่ม B เก็บไว้เป็นข้อเสนอเชิง data governance ต่างหาก
- ไม่ fine-tune โมเดล ใช้ pretrained model ตรงๆ เพราะ data ขนาดนี้ไม่จำเป็นต้อง train ใหม่ (data เยอะขึ้น = coverage เยอะขึ้น ไม่ใช่โมเดลฉลาดขึ้น)

## สถานะการพัฒนา (ล่าสุด)

- ✅ `catalog.json` — 5 รายการ (MEAT ZERO x2, Vg4love x3) ครบ field: name, brand, category, description, diet_tags, price
- ✅ `search.py` — รันได้จริง ใช้โมเดล `paraphrase-multilingual-MiniLM-L12-v2` คำนวณ embedding จาก name + description + category + diet_tags
- ✅ ยืนยันผลจริงแล้วว่า query ที่เว็บจริงหาไม่เจอ (เช่น "ของกินเจไม่ใช่ผัก") ระบบนี้หาเจอ

## To-do ก่อนพรีเซนต์วันศุกร์

- [ ] เพิ่มสินค้าให้ครบ 8-10 รายการ (ตอนนี้มี 5)
- [ ] สร้างตารางเทียบ "ALL Online เจอไหม" vs "ระบบเราเจอไหม" จาก query ที่เทสมาแล้ว
- [ ] เรียงสไลด์ pain point ทั้งชุดให้เป็นเรื่องเดียวกัน (ปัญหา → หลักฐาน → วิธีแก้ → ผลเทียบ → ข้อเสนอกลุ่ม B)
- [ ] ซ้อมพูด เผื่อคำถามแนว "ทำไมไม่เทรนโมเดลเอง" / "ต่อยอดยังไงต่อ"

## ชื่อโปรเจกต์ (พิจารณา)

- **รู้ใจ (Roo Jai)** — เข้าใจสิ่งที่ลูกค้าต้องการแม้พิมพ์ไม่ครบ/ไม่ตรงคำ (แนวทางที่แนะนำ)
- Meaning Match / CPF Smart Search — ตรงไปตรงมากว่า